// Package classifier implements the record-classification screen.
// Writes run off the Fyne goroutine in keystroke order (GoSerial);
// reads are synchronous against the local database.
package classifier
